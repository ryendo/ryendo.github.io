---
import BaseLayout from "../../../layouts/BaseLayout.astro";

type Post = {
  url: string;
  frontmatter: {
    title: string;
    description: string;
    date: string;
  };
};

const base = import.meta.env.BASE_URL;
const homeLink = `${base}ja/`;
const blogLink = `${base}ja/blog/`;
const altLink = `${base}en/blog/`;

const mdModules = import.meta.glob<any>("./*.md", { eager: true });
const astroModules = import.meta.glob<any>("./*.astro", { eager: true });

const posts: Post[] = [
  ...Object.entries(mdModules).map(([file, mod]) => ({
    url: mod.url ?? file.replace(/^\.\//, `${blogLink}`).replace(/\.md$/, "/"),
    frontmatter: mod.frontmatter,
  })),
  ...Object.entries(astroModules)
    .filter(([file]) => !file.endsWith("index.astro"))
    .map(([file, mod]) => ({
      url: `${blogLink}${file.replace(/^\.\//, "").replace(/\.astro$/, "/")}`,
      frontmatter: mod.frontmatter,
    })),
]
  .filter((p) => p.frontmatter && p.frontmatter.date)
  .sort(
    (a, b) =>
      new Date(b.frontmatter.date).getTime() -
      new Date(a.frontmatter.date).getTime()
  );
---

<BaseLayout
  title="ブログ"
  description="研究の更新、短いメモ、告知、セミナーや出張の記録"
  lang="ja"
  homeLink={homeLink}
  homeLabel="プロフィール"
  blogLink={blogLink}
  blogLabel="ブログ"
  altLink={altLink}
  currentSection="blog"
>
  <main class="main-panel">
    <h1>ブログ</h1>
    <p class="muted">研究の更新、短いメモ、告知、セミナーや出張の記録を書く場所です。</p>

    <ul class="blog-list">
      {posts.map((post) => (
        <li>
          <a href={post.url}>{post.frontmatter.title}</a>
          <div class="desc">{post.frontmatter.description}</div>
          <div class="date">{post.frontmatter.date}</div>
        </li>
      ))}
    </ul>
  </main>
</BaseLayout>
